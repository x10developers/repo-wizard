/**
 * Runs every 2 minutes.
 * Matches the minimum reminder constraints and reduces resource usage.
 */
cron.schedule("*/2 * * * *", async () => {
  const reminders = loadReminders();
  if (reminders.length === 0) return;

  // rest of your logic
});
